@model IEnumerable<AppProject.Models.Trainee>

@{
    ViewData["Record"] = "Record";
}

@{ var list = Model.ToList();

   for(int i =0; i<list.Count();i++)
       {
 <div class="no-break">
    <div class="container my-7">
            <div class="row">
                <div class="col-md-12">
                    <h4 class="text-center my-5">Conduct Record</h4>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12" id="top">
                <table class="table ">
                   <thead>
                      <!--   <tr>
                            <th class="text-center align-middle">ID</th>
                            <th class="text-center align-middle">Name</th>
                            <th class="text-center align-middle">Subject</th>
                            <th class="text-center align-middle">College</th>
                            <th class="text-center align-middle">Photo</th>
                        </tr> -->
                    </thead> 
                    <tbody>
                        <tr>
                            <td class="text-center">@Html.DisplayFor(modelItem => list[i].TraineeSerial)</td>
                            <td class="text-left">@Html.DisplayFor(modelItem => list[i].Name)</td>
                            <td class="text-left">@Html.DisplayFor(modelItem => list[i].Subject)</td>
                            <td class="text-left">@Html.DisplayFor(modelItem => list[i].WorkplaceAddress)</td>
                            <td class="text-right"> <img src="@Html.DisplayFor(modelItem => list[i].ProfileImagePath)" width="180" height="150" /></td>
                           
                        </tr>
                    </tbody>

                </table>
            
            </div>
            </div>
            
            <div class="row">
            <div class="col-md-12 details" id="details">
            <table class="table table-bordered" style="width:100%" >
                <thead>
                    <tr>
                        <th class="text-center">Date</th>
                       
                        <th class="text-center px-5" >Description</th>
                        <th class="text-center px-0">Remarks & Signature</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td style="height:1200px"></td>
                    
                        <td></td>
                        <td></td>
                    </tr>
                </tbody>

            </table>
            </div>
            </div>
        </div>
    </div>
     
       }
}